import React from 'react';

import { WebflowJs } from './assets/utils';

const CardBlurb = () => {
  WebflowJs(); //eslint-disable-line

  return (
    <p className="content-article news__content">The California Department of Public Health (CDPH), has been carrying out air sampling in vape shops throughout the state, as part of its initiative to determine the health effects of second-hand vapor exposure.
    </p>
  );
};


export default CardBlurb;
